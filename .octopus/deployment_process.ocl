step "blueprint" {
    name = "Blueprint"

    action {
        action_type = "Octopus.Blueprint"
        properties = {
            Octopus.Action.RunOnServer = "false"
        }
        worker_pool_variable = ""
    }
}

step "blueprintz" {
    name = "Blueprintz"

    action {
        action_type = "Octopus.Blueprint"
        properties = {
            Octopus.Action.RunOnServer = "false"
        }
        worker_pool_variable = ""
    }
}